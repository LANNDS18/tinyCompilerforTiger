let
  var i := 10
  var c := 11
  type person = {name: string, age: int}
  var unknownPerson: person := nil
in
  let
    var a := 1000
  in
    if i <>c & i>0 then (print("i and c are not equal, i = "); puti(i); print("\n")) else print("is equal");
    if i>0 | c>0 then (print("i or c > 0")) else print("i and c < 0")
  end
end

